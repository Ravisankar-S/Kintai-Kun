<div class="max-w-6xl mx-auto py-10 px-4">

  <!-- Greeting Section -->
  <div class="mb-8">

    <h1 class="text-4xl font-bold tracking-tight text-slate-800">
      <%= greeting_by_time %>,
      <span class="text-emerald-600">
        <%= current_user.name %>
      </span>
    </h1>

    <p class="text-slate-500 mt-2 text-sm">
      <%= Date.today.strftime("%A, %B %d, %Y") %>
    </p>

  </div>

  <!-- Zangyo Alert -->
  <%= render "shared/zangyo_alert" %>

  <!-- Main Status Card -->
  <div class="bg-white border border-slate-200
              rounded-3xl shadow-sm p-8 mb-8">

    <div class="flex items-start justify-between">

      <div>

        <% if @active_log %>

          <p class="text-emerald-600 font-semibold text-lg">
            🟢 Currently Working
          </p>

          <p class="text-slate-500 mt-2">
            Since
            <span class="font-medium text-slate-700">
              <%= @active_log.clocked_in_at.strftime("%I:%M %p") %>
            </span>
          </p>

        <% else %>

          <p class="text-slate-700 font-semibold text-lg">
            ⚪ Not Clocked In
          </p>

          <p class="text-slate-500 mt-2">
            Ready to start your shift.
          </p>

        <% end %>

      </div>

      <div>

        <% if @active_log %>

          <%= button_to "Clock Out",
              "/clock_out",
              method: :post,
              class: "bg-rose-500 hover:bg-rose-600
                      text-white px-8 py-3 rounded-2xl
                      font-semibold transition shadow-sm" %>

        <% else %>

          <%= button_to "Clock In",
              "/clock_in",
              method: :post,
              class: "bg-emerald-500 hover:bg-emerald-600
                      text-white px-8 py-3 rounded-2xl
                      font-semibold transition shadow-sm" %>

        <% end %>

      </div>

    </div>

  </div>

  <!-- Analytics Cards -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

    <!-- Today Card -->
    <div class="bg-white border border-slate-200
                rounded-3xl shadow-sm p-6">

      <p class="text-sm text-slate-500 mb-3">
        Today's Hours
      </p>

      <p class="text-4xl font-bold text-slate-800">
        <%= minutes_to_hm(@today_minutes) %>
      </p>

      <div class="mt-4 h-2 bg-slate-100 rounded-full overflow-hidden">

        <div class="h-full bg-emerald-500 rounded-full"
             style="width: <%= [(@today_minutes / 480.0 * 100), 100].min %>%">
        </div>

      </div>

      <p class="text-xs text-slate-400 mt-2">
        8-hour target
      </p>

    </div>

    <!-- Week Card -->
    <div class="bg-white border border-slate-200
                rounded-3xl shadow-sm p-6">

      <p class="text-sm text-slate-500 mb-3">
        This Week
      </p>

      <p class="text-4xl font-bold text-slate-800">
        <%= minutes_to_hm(@week_minutes) %>
      </p>

      <p class="text-sm text-slate-500 mt-4">
        Total accumulated work time this week.
      </p>

    </div>

  </div>

  <!-- Weekly Summary -->
<div class="mt-10">

  <div class="flex items-center justify-between mb-5">

    <h2 class="text-2xl font-semibold text-slate-800">
      Weekly Summary
    </h2>

    <p class="text-sm text-slate-500">
      Mon → Sun
    </p>

  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

    <!-- Days Worked -->
    <div class="bg-white border border-slate-200
                rounded-3xl p-6 shadow-sm">

      <p class="text-sm text-slate-500 mb-2">
        Days Worked
      </p>

      <p class="text-4xl font-bold text-slate-800">
        <%= @weekly_summary[:days_worked] %>
      </p>

    </div>

    <!-- Total Hours -->
    <div class="bg-white border border-slate-200
                rounded-3xl p-6 shadow-sm">

      <p class="text-sm text-slate-500 mb-2">
        Total Hours
      </p>

      <p class="text-4xl font-bold text-slate-800">
        <%= minutes_to_hm(
              @weekly_summary[:total_minutes]
            ) %>
      </p>

    </div>

    <!-- Average -->
    <div class="bg-white border border-slate-200
                rounded-3xl p-6 shadow-sm">

      <p class="text-sm text-slate-500 mb-2">
        Average Per Day
      </p>

      <p class="text-4xl font-bold text-slate-800">
        <%= minutes_to_hm(
              @weekly_summary[:average_minutes]
            ) %>
      </p>

    </div>

    <!-- Overtime -->
    <div class="bg-white border border-slate-200
                rounded-3xl p-6 shadow-sm">

      <p class="text-sm text-slate-500 mb-2">
        Overtime Days
      </p>

      <p class="text-4xl font-bold text-rose-500">
        <%= @weekly_summary[:overtime_days] %>
      </p>

    </div>

  </div>

</div>

<%= render "shared/heatmap" %>

</div>