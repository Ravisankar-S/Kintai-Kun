<%# This file is intentionally left empty.
    Pagination is rendered inline in each view using @collection.current_page,
    @collection.total_pages, first_page?, and last_page? — methods kaminari
    attaches directly to the paginated ActiveRecord relation.
    See app/views/work_logs/index.html.erb for the implementation. %>
